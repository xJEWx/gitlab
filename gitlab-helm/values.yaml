global:
  hosts:
    domain: gitlab-stand.pg
    gitlab:
      name: gitlab-stand.pg
  ingress:
    enabled: true
    tls:
      enabled: false  # Отключаем TLS
    annotations:
      kubernetes.io/ingress.class: nginx

# Полное отключение cert-manager
certmanager:
  install: false
certmanager-issuer:
  install: false

# Остальные настройки остаются без изменений
postgresql:
  install: true
  persistence:
    size: 20Gi

redis:
  install: true
  persistence:
    size: 10Gi

gitlab-runner:
  install: true
  runners:
    privileged: true

gitlab:
  webservice:
    replicas: 2
  sidekiq:
    replicas: 2
